import 'package:flutter/material.dart';

class AppColors {
  // Paleta Monocromática Minimalista (Blanco, Negro y Escala de Grises)
  static const Color primary = Color(0xFF000000); // Negro puro para acento principal y contrastes
  static const Color primaryLight = Color(0xFF2C2C2E); // Carbón intermedio
  static const Color primaryDark = Color(0xFF000000);

  static const Color secondary = Color(0xFF3A3A3C); // Gris oscuro de soporte
  static const Color success = Color(0xFF1C1C1E); // Tono monocromático elegante

  // Fondos y Superficies
  static const Color background = Color(0xFFFAFAFA); // Off-white ultra limpio
  static const Color surface = Colors.white; // Blanco puro
  static const Color surfaceElevated = Color(0xFFF2F2F7); // Gris claro de superficie
  static const Color cardSurface = Colors.white;
  static const Color border = Color(0xFFE5E5EA); // Gris claro para bordes finos
  static const Color borderFocused = Color(0xFF000000); // Borde negro al enfocar

  // Errores
  static const Color error = Color(0xFFE02020);
  static const Color errorLight = Color(0xFFFFF0F0);

  // Tipografías
  static const Color textPrimary = Color(0xFF111111); // Negro profundo de lectura
  static const Color textSecondary = Color(0xFF6E6E73); // Gris medio sofisticado
  static const Color textMuted = Color(0xFFA1A1A6); // Gris claro de apoyo
  static const Color textLight = Colors.white; // Blanco
}
